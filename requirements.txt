# ==============================================================================
# CryptoIA Trading Bot - Dependencias
# Instalar con: pip install -r requirements.txt
# ==============================================================================

# --- Conexión a exchanges de criptomonedas ---
ccxt>=4.0.0

# --- Scraping de noticias RSS ---
feedparser>=6.0.0

# --- Llamadas HTTP (a Ollama y APIs externas) ---
requests>=2.31.0

# --- ORM para base de datos (PostgreSQL) ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# --- Variables de entorno ---
python-dotenv>=1.0.0

# --- Consola con colores, tablas y paneles ---
rich>=13.0.0

# --- Análisis de datos ---
pandas>=2.0.0
numpy>=1.24.0

# --- Indicadores técnicos (RSI, MACD, Bollinger, EMA, etc.) ---
ta>=0.11.0

# --- API REST (FastAPI + servidor ASGI) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Zona horaria (Python 3.9+, incluido en stdlib pero por si acaso) ---
# zoneinfo ya viene en Python 3.9+, no requiere instalación extra
